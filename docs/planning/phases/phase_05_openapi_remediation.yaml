id: phase_05_openapi_remediation
title: "OpenAPI Specification & Governance Remediation"
status: IN_PROGRESS
created: "2026-03-29"
updated: "2026-03-29"
owner: "niq-tigerteam"
description: >
  Remediation phase generated by the NizamIQ OpenAPI Assessment & Governance
  Remediation Agent. Covers OpenAPI spec generation, UDS compliance, and
  planning artefact scaffolding for Plaza.

objectives:
  - Generate accurate OpenAPI specification matching runtime implementation
  - Fix UDS frontmatter compliance across all docs/ markdown files
  - Scaffold missing planning artefacts (manifest.json, DEBT.md, AGENTS.md)
  - Achieve API completeness score >= 9/10

tasks:
  - id: task_01
    title: "Review and validate generated OpenAPI spec"
    status: TODO
    priority: HIGH
    description: >
      Human review of the auto-generated openapi.yaml to verify all endpoints,
      request/response schemas, and security definitions are accurate.
    steps:
      - Validate GET /api/v1/health (Health check)
      - Validate GET /api/v1/dashboard (Get dashboard data)
      - Validate GET /api/v1/profile (Get user profile)
      - Validate PUT /api/v1/profile (Update user profile)

  - id: task_02
    title: "Expand OpenAPI schemas with full request/response models"
    status: TODO
    priority: HIGH
    description: >
      Replace generic SuccessResponse schemas with precise Pydantic-derived
      models for each endpoint. Add example values for all fields.

  - id: task_03
    title: "Add OpenAPI spec to CI validation pipeline"
    status: TODO
    priority: MEDIUM
    description: >
      Integrate spectral or openapi-validator into the CI pipeline to prevent
      spec drift. Add a pre-commit hook to validate on every commit.

  - id: task_04
    title: "Publish spec to Atlas contract registry"
    status: TODO
    priority: MEDIUM
    description: >
      Register the validated OpenAPI spec with the Atlas API contract registry
      to enable ecosystem-wide contract testing and compatibility checking.

dependencies:
  - Atlas (contract registry)
  - documentation-standard (UDS schema)

acceptance_criteria:
  - openapi.yaml passes spectral lint with zero errors
  - All endpoints have full request/response schemas
  - Spec is registered in Atlas
  - CI pipeline validates spec on every PR
