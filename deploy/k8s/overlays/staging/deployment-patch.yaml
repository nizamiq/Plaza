apiVersion: apps/v1
kind: Deployment
metadata:
  name: plaza
  namespace: plaza
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: plaza
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 2Gi
